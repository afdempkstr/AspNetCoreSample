@model CandidateListViewModel
@inject IViewLocalizer Localizer

<h1>@Model.CurrentSpecialisation</h1>

@foreach (var candidate in Model.Candidates)
{
    <partial name="_CandidateInfo" model="candidate" />
}

<h3>@Localizer["Good Morning"]</h3>
